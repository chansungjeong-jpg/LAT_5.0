# 일일 업무보고 — 2026-08-14

## 오늘 작업 결과

- 일봉 RSI(14) 수치 표시를 유지했다.
- 60분봉 RSI(14) 수치를 위치 분석 context와 Markdown 리포트에 추가했다.
- RSI는 보조지표로만 사용하며 점수·게이트·WATCH·Hard Block·`entry_eligible`에 사용하지 않는다.
- 종목별 매수 위치 순위 모듈을 구현했다.
- `entry_eligible=true` 후보를 위치 점수 내림차순으로 우선 정렬한다.
- 진입 불가 또는 점수 미확정 종목은 임의 점수 없이 하단에 보존한다.
- 테마별 랭킹과 대시보드는 보류했다.

## 검증

- 집중 테스트: `83 passed`
- 전체 회귀 테스트: `253 passed`
- `compileall` 및 `git diff --check`: 통과
- RSI 계산: Wilder RSI(14), 완료봉·no-lookahead 유지
- 주문·알림·스케줄러: 변경하지 않음

## 실제 DB 검증

- 입력: `data/lat5_market.db`, Watchlist 154종목
- 60분봉·5분봉 커버리지: 각각 154종목
- 강한 돌파 1,003건, 눌림 301건, 반전 트리거 0건
- 위치 판정 301건, `entry_eligible=true` 0건
- 일봉 RSI 유효 301건, 60분봉 RSI 유효 301건
- 거래 0건은 오류가 아니라 위치 필터와 반전 트리거 조건을 통과한 종목이 없다는 결과다.
- 주요 차단 사유: `RR_TOO_LOW` 262건, `DAILY_MA_HARD_BLOCK` 36건, `DAILY_MA_WATCH_PRESSURE` 26건

## 필터 OFF 기준선 진단

- 위치 필터를 끈 별도 기준선에서 5분봉 반전 트리거 97건을 확인했다.
- 체결 97건, 승률 25.77%, Profit Factor 0.588, 기대손익 -42,529원, 최대낙폭 4,228,564원이다.
- 따라서 반전 트리거 자체가 없는 문제가 아니며, 현재 위치 필터가 301건을 모두 차단한 결과다.
- 기준선 성과가 음수이므로 조건을 완화하거나 실매매로 연결하지 않는다.

## 데이터 상태

- DB 최신 일봉·분봉: `2026-08-07`로 현재 기준 최신성이 부족하다.
- `sector_score` 계산·저장 경로가 없어 301건 모두 미확정이다.
- 이전 수집 실행은 PARTIAL이었으며 페이지 제한 오류가 확인됐다. 이후 페이지 처리 수정으로 재수집했다.

## 시장 데이터 재수집 결과

- Run 9는 `COMPLETE`로 종료됐다. 대상 154종목, 성공 154종목, 오류 0건이다.
- `ka10080`·`ka10059` 페이지 처리 수정 후 Probe와 전체 수집 모두 성공했다.
- 최신 일봉은 `2026-08-14`, 최신 5분봉은 `2026-08-14 14:10`까지 갱신됐다.
- 데이터 건강성 검사는 `PASS`이며, 이번에는 수집 Run도 `COMPLETE`다.

## 최신 데이터 스코어링

- 위치 판정 316건, 일봉·60분봉 RSI 각각 316건 정상
- 일봉 5일선 이격도 보조 점수 추가: 최근 3개 완료봉의 이격도 감소 시 최대 `+10`, 확대 시 최대 `-10`
- 최신 분포: `+10` 66건, `+7` 34건, `+4` 13건, `0` 99건, `-5` 58건, `-10` 46건
- `entry_eligible=true` 1건, 거래 0건
- 주요 차단: `RR_TOO_LOW` 270건, `DAILY_MA_HARD_BLOCK` 38건
- 데이터 수집 오류가 아닌 위치·손익비 Hard Block 결과이므로 임의 완화하지 않는다.

## 다음 작업

1. `sector_score`의 SSOT·계산식·point-in-time 입력을 먼저 확정
2. 최신 데이터 기준 필터 ON/OFF 성과를 동일 기간으로 재검증
3. `entry_eligible` 0건의 손익비·이평선 차단 분포를 종목별 분석

## 보류

- 테마별 종목 랭킹
- 대시보드
- RSI 자동 점수화·자동 매매 판단
