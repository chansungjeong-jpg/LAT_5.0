# 일일 업무보고 — 2026-08-27

## 오늘 한 일

### 1. 파이프라인 크래시 2건 발견·수정

**네트워크 타임아웃 크래시** (`a32d0b6`)

오늘 16:00 스케줄 실행이 첫 키움 API 호출에서 `requests.exceptions.ReadTimeout`을
맞고 프로세스째 죽었다. 154종목 중 단 한 종목도 수집하지 못했다. 429·5xx는 이미
재시도 로직이 있었지만, 연결 단계 타임아웃은 `status_code` 검사에 도달하지도
못해 재시도 경로를 통째로 우회했다. `session.post` 자체를 같은 재시도 루프로
감싸고, `max_attempts` 소진 후에만 `KiwoomApiError`(collector가 정상 처리하는
예외)를 던지도록 수정했다.

**로그 버퍼링** (`a119615`)

파이프라인 로그가 기본 버퍼링이라 with 블록이 닫힐 때까지 디스크에 아무것도
쓰이지 않았다. 실행 중이거나 하드 크래시가 나면 로그 파일이 비어 보여서 오늘
타임아웃 진단이 실제보다 오래 걸렸다. 명령마다 `flush()` 추가.

### 2. 상대강도(RS) 대시보드 연동 (`2cd2bd1`)

`relative_strength_scan.py`가 마크다운 리포트와 함께 JSON 사이드카
(`artifacts/latest_scoring/relative_strength.json`)를 쓰도록 하고,
`build_dashboard.py`가 이를 읽어 위치점수 표 아래에 RS Top10 섹션을 렌더링한다.
JSON이 없으면 아무것도 그리지 않는 선택적 연동이다. "진짜 지수가 아니라
Watchlist 바스켓 대리값"이라는 단서도 대시보드에 함께 표시된다.

### 3. 08-21 파이프라인 복구 (`07940db`)

08-21 16:00 스케줄이 실행되지 않았고, 22:14 시도는 `0x800710E0`
(operator/administrator refused the request)로 실패했다. 이 태스크의
`Interactive only` 로그온 모드에서 트리거 시점에 대화형 세션이 없을 때 나오는
전형적 증상이다. 동일 명령을 수동 재현해 정상 완료를 확인했다 — 파이프라인
코드 자체는 무관한 스케줄러/세션 문제였다.

## 검증

- 전체 테스트: `303 passed` (신규 네트워크 타임아웃 회귀 테스트 2건 포함)
- `compileall`: 통과
- 오늘 파이프라인 재실행: 수집 154/154(오류 0, run 24) → data-health `PASS` →
  필터 27종목 → 추세선 스캔 `signals=0` → 백테스트 → 대시보드 갱신, 전 단계
  `COMPLETE`

## 오늘 종목 결과 (2026-08-27)

- 1차 필터(월봉10·주봉5 회복) 통과: **27종목**
- 60분 추세선 신호: **0건** — Hard Block이 아니라 셋업 자체가 없는 결과
- RS 상위(20거래일, 시장 대리값 +34.50%):
  RF머트리얼즈 +76.2%p, 대한광통신 +65.0%p, 엘앤에프 +63.4%p,
  티엘비 +59.4%p, 아스플로 +52.4%p

## 08-27 추가 진행 (이어서)

### 3. 좀비 run 수정 (`6dae77d`)

타임아웃 크래시(위 1번)가 `_run_collect`에서 `KiwoomTokenError`만 잡던 구멍도
같이 드러냈다 — 예상 못 한 예외가 나면 run이 `RUNNING` 상태로 영원히 박제된다.
run 23이 정확히 이 상태였다. 일반 예외도 잡아 `finish_run(BLOCKED)`로 마무리한
뒤 재-raise하도록 수정(트레이스백은 그대로 보존, 장부만 닫는다). 회귀 테스트
추가, run 23은 백업 후 수동으로 `BLOCKED`로 정정(`reports/data_health/
run23_before_fix_backup.json`). data-health는 run 24 기준 계속 `PASS`.

### 08-22~26 공백 확인 결과 → **공백 아님**

`ohlcv_daily` 실데이터 확인 결과 08-24·25·26 전부 154종목 커버리지 정상.
08-22·23은 주말이라 원래 거래일이 아니다. 오늘(08-27) 재수집이 과거 이력을
함께 채워 넣어 소급 수집이 필요 없었다. 파이프라인 로그 부재는 스케줄 미실행의
증거였을 뿐 데이터 자체는 비어있지 않았다.

## 내일 할 일

1. ~~스케줄러 세션 문제 결정~~ — **완료(08-30, `311df00` 이전 별도 조치)**.
   `schtasks /Change /TN LAT5_Monthly10_Weekly5_Recovery /RU SYSTEM`(관리자
   권한 필요)으로 전환. `Run As User: SYSTEM`, `Logon Mode: Interactive/
   Background`로 확인됨 — 로그인 여부와 무관하게 실행되고 개인 Windows
   비밀번호도 저장하지 않는다. **검증은 08-31(월) 16:00 첫 실행에서** —
   SYSTEM 계정이 `.env`/토큰 캐시 파일을 읽을 수 있는지(NTFS 권한) 그때
   확인해야 한다.
2. ~~RS 스캔 파이프라인 편입 여부 결정~~ — **완료(`311df00`)**. 백테스트 다음,
   대시보드 재생성 전 단계로 편입. end-to-end 재검증 완료(305 tests passed,
   `output=reports\relative_strength\2026-08-27.md` 확인).
3. ~~08-22~26 공백 확인~~ — **완료(위 참조), 공백 아니었음.**
4. **(이월) `sector_score` 미구현** — final_spec 5장 Sector Flow Gate가 여전히
   코드에 없어 모든 위치 판정에서 `sector_score`가 UNKNOWN으로 남는다. 별도
   설계 세션 필요.
5. **(신규) 08-28 스케줄 미실행 확인** — 08-28(금)은 평일인데 파이프라인 로그가
   없다. SYSTEM 전환 전이라 `Interactive only` 제약으로 미실행됐을 가능성이
   높다(1번과 같은 원인) — 08-31 실행이 정상화되면 재발 여부로 확정.

## 보류

- 진짜 KOSPI/KOSDAQ 지수 데이터 — `ka10081`에 지수코드(001/101)를 넣으면 빈 값이
  오고, 키움 REST 문서 포털이 JS SPA라 지수 차트 TR 코드를 확인하지 못했다.
  현재는 Watchlist 154종목 동일가중 평균을 시장 대리값으로 사용한다
  (`market_proxy_method=watchlist_equal_weight_v1`). `market_average_return()`
  함수만 교체하면 되도록 분리해 두었다.
