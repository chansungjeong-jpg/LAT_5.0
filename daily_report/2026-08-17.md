# 일일 업무보고 — 2026-08-17

## 오늘 작업 결과

- `000720`(현대건설) 반복 데이터 손상 원인을 규명했다. 매 전체수집이 1986년
  상장 초기 이력까지 무제한 재수집하다 소스 API의 손상행(1986-03-29)에
  걸리는 구조였다. 일봉 수집(`ka10081`)에 기존 수급(`ka10059`) 365일
  컷오프와 동일한 방식으로 2년 컷오프를 추가해 재발을 차단했다.
- `run_daily_signal_reports.py`의 로그 기록을 `"w"`(덮어쓰기)에서
  `"a"`(추가) 모드로 바꿨다. 수동 실행이 스케줄 실행 로그를 지워버리는
  구조였다.
- 미커밋 상태였던 핵심 기능(월봉10·주봉5 회복 필터, 60분 트렌드라인
  추세선/이평 돌파 스코어링) 코드·테스트·리포트를 커밋했다.
- 시스템 목적을 `LAT_SIMPLE_v1_0_final_spec.md` 최상단에 명시했다:
  매수 여부를 대신 정하는 시스템이 아니라 근거를 보여주는 관찰·필터
  도구라는 원칙.
- 위치 점수의 거래량 항목을 "직전 7거래일(당일 제외) 평균 대비 비율"
  기준으로 변경했다(기존 60분봉 불리언 3플래그 방식 폐기). 스마트머니
  유입을 단일 시점이 아닌 최근 평균 대비로 판단하기 위함이며, 버킷
  점수(0/5/10/15/20/30)는 final_spec 표를 그대로 재사용했다.
- Windows 작업 스케줄러가 `PYTHONPATH`를 설정하지 않아 매일 16:00
  스케줄 실행이 `ModuleNotFoundError`로 즉시 차단되던 문제를 로그 개선
  덕에 발견하고, 하위 프로세스에 `PYTHONPATH`를 명시 주입하도록 수정했다.

## 검증

- 전체 154종목 재수집: `COMPLETE`(run 12, run 13), 에러 0건
- data-health: `PASS`
- 전체 회귀 테스트: `273 passed`, `compileall` 통과
- `PYTHONPATH` 미설정 상태(스케줄 태스크와 동일 조건)로 수집→건강성→필터→
  랭킹 전체 파이프라인 재현 검증 → `COMPLETE`

## 오늘자 종목 스코어링

- 월봉10·주봉5 회복 1차 필터: 154종목 중 29종목 통과(전부 "잠정" 상태)
- 60분 트렌드라인 돌파 스캔(`scan_trendline_rank.py`): `signals=0` —
  Hard Block이 아니라 순수하게 셋업 자체가 없는 결과
- 위치 점수 직접 재계산(29종목 전수, 08-17 종가 기준): 전종목
  `NO_PULLBACK` 차단으로 `entry_eligible=false`. 최고점은 현대무벡스
  (319400)·티씨케이(064760) 66점 — 점수 순위는 존재하나 매수 후보는 0개

## 커밋

- `0246f21` fix: bound daily OHLCV pagination and harden pipeline logging
- `83c3105` docs: record clean full-scope collection and passing pipeline
- `c67ee7b` feat: score daily volume against prior-7-session average
- `db253ff` fix: inject PYTHONPATH for daily pipeline subprocesses
- 브랜치 `agent/daily-ma-reaction-score`, 원격 푸시 완료

## 다음 작업

1. `sector_score` 계산·저장 경로가 여전히 없어 모든 위치 판정에서
   `sector_score`가 UNKNOWN으로 남는다 — SSOT·point-in-time 입력 확정 필요
2. 오늘 작성한 위치 점수 재계산 스크립트(스크래치 영역)를 정식
   `scripts/` 도구로 편입할지 결정
3. 08-18 장중/마감 데이터로 오늘 순위 재검증

## 보류

- 테마별 종목 랭킹
- 대시보드
- RSI 자동 점수화·자동 매매 판단
